# MH-21 Contradiction Register

Status: ACTIVE.

C21-001: provider `remote/local/hybrid` is a compute classification, not trust/authorization. RESOLVED ARCHITECTURALLY.

C21-002: adapter endpoint is configuration, not permission; egress policy decides eligibility. RESOLVED ARCHITECTURALLY.

C21-003: AI response output is unconstrained data; it cannot be interpreted as authority. CONTROL REQUIRED.

C21-004: current runtime absence cannot be claimed from code-search alone. UNKNOWN / REQUIRES VERIFICATION.