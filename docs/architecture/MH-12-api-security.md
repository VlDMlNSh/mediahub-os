# MH-12 API Security

All local, remote, admin, diagnostic, plugin, AI, device and cloud APIs require authentication, operation-specific authorization, schema/input validation, payload limits, replay/idempotency controls where applicable, rate limits, timeout/cancellation, bounded errors, correlation and audit.

APIs cannot bypass the Consumer Boundary or State Authority contract.
