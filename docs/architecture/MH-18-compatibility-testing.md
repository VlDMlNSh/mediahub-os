# MH-18 — Compatibility Testing
Status: PROPOSED / NOT ACCEPTED

Compatibility matrix must cover supported OS/device classes, media containers/codecs, metadata encodings, subtitle formats, storage backends and provider protocols. Each result is scoped to exact versions/configuration. Unsupported media must fail predictably without corrupting catalog. Vendor behavior requires direct evidence.