# MH-21 Model Routing Policy

Status: PROPOSED.

Eligibility is determined by task + data class + security/privacy policy + model capability + resource availability + cost + latency + reliability. Provider A failure cannot implicitly select arbitrary B. Only an explicit policy-approved fallback set is eligible.