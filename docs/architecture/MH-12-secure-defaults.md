# MH-12 Secure Defaults

Defaults: deny-by-default, least privilege, explicit trust, explicit authorization, fail-closed, bounded resources, secure errors and no implicit local/network trust.

Unknown identity, unavailable authentication, malformed input, ambiguous policy, invalid credential, expired certificate or corrupt security metadata results in DENY, QUARANTINE or SAFE DEGRADATION according to operation.
