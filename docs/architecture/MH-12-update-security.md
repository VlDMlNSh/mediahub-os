# MH-12 Update Security

Updates require authenticated source, artifact integrity, signing/provenance verification, compatibility checks, version policy, downgrade protection, controlled rollback, health gates and recovery compatibility.

Host/application/data/security updates are distinct. An update cannot silently increase privilege or create a new authority path.
