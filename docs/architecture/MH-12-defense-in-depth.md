# MH-12 Defense in Depth

Security layers: identity; authentication; authorization; capability; policy; boundary enforcement; isolation; input validation; resource limits; cryptographic integrity; audit; observability; incident response; recovery.

Compromise of one layer or failure domain must not automatically grant system authority.
