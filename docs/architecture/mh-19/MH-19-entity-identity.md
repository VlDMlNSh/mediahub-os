# MH-19 Entity Identity
Status: PROPOSED

Entity identity must be distinct from physical, network, storage and source identifiers. Candidate fields: canonical ID, external IDs, source IDs, aliases, lifecycle and provenance. Existing Device mediahub_id is a strong foundation; no second canonical identity namespace without ADR.