# MH-21 Cloud Failure

Status: PROPOSED.

Assume timeout, outage, malformed/stale/hallucinated output, API/schema change, rate limiting, pricing change, compromise and credential failure. Cloud failure must not transfer authority or disable local critical control.

Fallback: approved remote alternative → local AI → deterministic logic → manual/safe state, subject to policy. No arbitrary provider fallback.