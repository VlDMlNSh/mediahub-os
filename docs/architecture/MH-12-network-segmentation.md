# MH-12 Network Segmentation

Segmentation is a defense-in-depth boundary between runtime, management, device, recovery and external networks. A segment never becomes trusted solely by topology or VPN membership.

Segmentation must reduce blast radius, constrain ingress/egress and preserve explicit application-layer identity and authorization. Exact VLAN/IP/port topology is REQUIRES VERIFICATION.
