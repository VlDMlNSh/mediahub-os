# MH-21 Cloud Credentials

Status: PROPOSED.

Separate MediaHub identity, user identity, workload identity, service identity and provider credentials. Credentials must be least-privilege, scoped, rotatable, observable and auditable. AI/agents never receive master credentials or unrestricted device credentials.