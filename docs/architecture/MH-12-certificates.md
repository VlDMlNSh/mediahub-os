# MH-12 Certificates

Certificates are identity/integrity infrastructure, not authorization by themselves. Validation must cover trust anchor, chain, validity, identity binding, expiry, revocation/rotation and compromise handling.

Certificate lifecycle must be explicit, auditable and compatible with recovery. Exact CA and validation implementation remain REQUIRES VERIFICATION.
