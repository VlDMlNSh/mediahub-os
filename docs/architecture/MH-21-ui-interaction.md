# MH-21 UI Interaction

Status: PROPOSED.

UI should indicate workload location: Local, Remote, Provider, Blocked or Degraded. For sensitive workloads, expose destination and provider/model and applicable data-handling/routing reason where policy permits. UI display does not itself grant authorization.