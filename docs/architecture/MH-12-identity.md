# MH-12 Identity

Canonical principal classes: human, service, device, plugin, AI/model, workload, recovery, update/signing, audit.

Identity is unique, lifecycle-managed and distinct from authorization. Lifecycle: enrollment → activation → suspension/revocation → rotation/expiration → recovery/compromise handling.

Identity context must remain bound to the principal and applicable security context. Unknown or unverifiable identity is not silently trusted.
