# Astra Agent Registry v0

Each registration contains stable agent id, capability set, implementation version, allowed tools, input/output schema version, policy profile, health state and concurrency limit.

Initial logical agents: architect, coding, review, test, security, devops, repository, reconcile.

The registry is authoritative for routing eligibility. Agent names do not grant permissions by themselves.
