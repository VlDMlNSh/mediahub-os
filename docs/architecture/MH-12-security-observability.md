# MH-12 Security Observability

Security enforces; observability reports security state; audit accounts; diagnostics investigates; incident response contains/recover; State Authority owns canonical mutation.

Observability must not become an enforcement or mutation authority. Security-sensitive telemetry is bounded, privacy-aware and must not expose secrets.
