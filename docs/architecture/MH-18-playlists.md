# MH-18 — Playlists / Collections
Status: PROPOSED / NOT ACCEPTED

Playlist = ordered explicit references. Queue = session playback ordering. Collection = curated grouping. Smart Collection = derived query unless explicitly modeled otherwise. Favorite/bookmark are user-level references. Membership changes require authorized mutation; smart results never become canonical membership without an explicit operation.