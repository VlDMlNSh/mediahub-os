# W04C — OpenRouter 403 remediation checkpoint

Date: 2026-09-10

## Objective

Remove OpenRouter from the native MediaHub Cloud Development Adapter execution path and prevent provider/geography failures from being misclassified as credential failures.

## Iteration 2 — OpenRouter elimination

- Native provider gateway implementation was reduced to qualified Opper and Continuum providers.
- OpenRouter credential loading was removed from `ops/systemd/mediahub-provider-gateway.service`.
- OpenRouter upstream routing branches were removed from `ops/mediahub_provider_gateway_http.py`.
- Gateway tests were migrated to exercise the remaining qualified providers while preserving failure classification and bounded failover coverage.
- Native adapter runtime no longer contains an OpenRouter-specific dependency/comment.
- `/home/mediahub/.codex/config.toml` on `mh-dev-01` now defaults to `model_provider = "continuum"` with `model = "gpt-5.6-luna"`.
- A sanitized MediaHub-specific Codex home exists at `/home/mediahub/.mediahub-codex`.
- The previous Codex configuration is retained only as a local non-runtime backup.

## Verified

- Full pytest: 300 passed.
- Ruff: all checks passed.
- Local security scan: exit code 0.
- pip-audit: no known vulnerabilities.
- Runtime OpenRouter scan under `ops/`: PASS.
- argv credential pattern scan: no matches.
- Codex CLI configuration/help parsing: PASS (`codex-cli 0.151.0`).
- Continuum is documented as serving the Responses API at `https://continuumcode.ai/v1`, which matches modern Codex `wire_api = "responses"`. citeturn0search2turn0search1

## Remaining gates

- The system `mediahub-openrouter-gateway.service` is still `active (running)`. A direct `systemctl stop` returned `Interactive authentication required`; no privileged/destructive system change was attempted.
- `mediahub-codex-lane.service` and `mediahub-claude-lane.service` are `inactive` and `static`.
- Mypy remains blocked by repository/module-resolution and missing-import configuration; no PASS is claimed.
- Semgrep reports four blocking findings, including the existing loopback HTTP local-AI rule and HTTPSConnection rule; no suppression was added.
- Bandit reports seven low-severity findings in existing local autonomous/security code; no false PASS is claimed.
- ECC cache/legacy state and local backup files can contain historical OpenRouter text; they are not native runtime authority.
- The local development commit is `acfe72a7fa2d93c2a106629c419bc5c2c725039f`, TREE `236fcdf09805bbf07f5805813a53f1db7ab4e76e`. Direct local push is unavailable on this host, so GitHub branch history does not yet contain this exact local commit.

## Immutable authority

R4 remains unchanged. No production merge, force-push, history rewrite, VPN, proxy, location spoofing, OpenRouter use, or credential forwarding was performed.
